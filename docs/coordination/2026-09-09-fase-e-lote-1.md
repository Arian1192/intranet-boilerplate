# Fase E · Lote 1 — Dashboard y Shows

**Ejecutor:** pane `wA:pC` (el que cerró las Fases B, C y G). **Coordinador:** `wA:p5`.
**Spec:** `/home/arian/dev/Boilerplate/docs/superpowers/specs/2026-09-09-conceptone-v3-design.md` §4.

**Rama:** `feature/conceptone-v3-recalco-e1`. **Worktree:** el tuyo, ya puesto en la rama por el coordinador.

## Por qué estas dos juntas

Los **6 KPI de pipeline** del Dashboard son agregados **sobre los shows**, y la tira termina en un enlace
`Ir a todos los shows →`. Si el Dashboard y `/shows` los escriben dos cabezas distintas, los números
dejan de cuadrar entre pantallas. Van juntas por eso.

Es el lote **más gordo de la Fase E**: el Dashboard estrena dos bloques que hoy **no existen** en nuestra
copia.

## Dashboard (`/conceptone`) — delta confirmado, no «revisar»

1. **Tira de 6 KPI de pipeline.** Medidos en el live el 2026-09-09:
   `TENTATIVE 70.479,07 € · 100` · `CONFIRMADO 14.100,56 € · 16` · `CONTRATO 30.905,46 € · 28` ·
   `PENDIENTE COBRO 32.473,28 € · 27` · `PENDIENTE LIQUIDAR 24.250,00 € · 19` · `CERRADO 31.350,00 € · 23`.
   Cada KPI lleva **importe y recuento**. Fija las cifras de tu captura y dilo en la PR.
2. **Enlace `Ir a todos los shows →`.**
3. **Feed `NOVEDADES`** con la bajada «Lo que hacen los promotores con sus correos y contratos»: eventos
   de firma **fechados**, con referencia tipo `C1-2026-158`.

**Ninguno de los tres existe hoy en nuestra copia.** Captura el feed a fondo: cuántos eventos trae, qué
tipos hay, cómo se formatea la fecha y qué pasa cuando está vacío.

## Shows (`/shows`) — marcada «revisar»

Toolbar del live: `87 shows` · `Solo futuros` · rango `Última semana → Todo el futuro` · `Filtros` ·
leyenda de estado. **Confirma el delta con un diff `ours/` ↔ live antes de escribir nada.**

Dos cosas que medir explícitamente, porque el resto de la ronda ha demostrado que no se pueden suponer:

- **¿El contador `87 shows` cuenta lo filtrado o el conjunto?** Pulsa `Solo futuros` y míralo.
- **El rango `Última semana → Todo el futuro`**: ¿son dos `select` nativos, `ApxDd`, o un `.dp`?

## Ojo con lo que ya hay

`/shows` y el detalle `/shows/:id` **ya existen y tienen tests** — de hecho las 243 filas de
`/liquidaciones` enlazan al detalle del show. No rompas esos enlaces.

## Qué es la Fase E

**Recalco de pantallas que YA existen.** No escribes una pantalla nueva: corriges la que tenemos contra
el live de hoy. Eso cambia el método en un punto importante: **esas páginas ya tienen tests**. Empieza
ejecutando los suyos y **entiéndelos antes de tocarlos**: si uno se pone rojo por tu cambio, decide a
conciencia si el test estaba fijando algo correcto que estás rompiendo, o algo que el live desmiente y
hay que actualizar. Nunca borres un test para que pase el build.

## El §4 del spec es un punto de partida, no la verdad

La tabla de deltas del §4 se levantó el 2026-09-09 y **ella misma marca tres pantallas como «revisar»**.
Tu primer bloque es **confirmar el delta con un diff `ours/` ↔ live en el momento de ejecutar**, no dar
por buena la tabla. Si el delta real es mayor o menor de lo que dice, **dilo y ajusta**; si encuentras
uno que la tabla no recoge, avísame.

## Lo que ya sabemos y no hay que redescubrir

- **`brand-*` se escribe tal cual.** Dentro de `.apx` el violeta lo pone `apx.css`. Hardcodearlo rompe el
  modo oscuro.
- **Los importes llevan espacio duro (U+00A0)**, medido dos veces. `formatImporte` de `cobros.ts` mete
  uno normal: comprueba cuál usa tu pantalla antes de fiarte.
- **Los contadores NO son uniformes.** En `/management/{campanas,activaciones,content,roster}` cuentan
  **lo filtrado**; en `/management/insights`, el conjunto. **Si tu pantalla tiene chips o contadores,
  MÍDELO, no lo supongas.**
- **Los literales del live mandan sobre los correctos.** Medido: el live escribe «1 artistas del roster»
  sin declinar. Se calca y se blinda con un test.
- **`ApxDd`** (`src/features/booking/components/ApxDd.tsx`) es el desplegable compartido, importado por
  ruta directa. Reutilízalo si encaja — pero los modales de Management usan **`select` nativo**: mira qué
  usa tu pantalla antes de elegir.
- **Salir de un estado de cliente se hace RECARGANDO, no deshaciendo.** Un modal, un calendario, un
  filtro escrito: todo muere con `page.reload()` sin tocar nada. Deshacer implica pulsar cosas, y ahí
  está el riesgo.

## Congelados y ficheros compartidos

`router.tsx`, `nav.ts`, `src/features/booking/shell/*` y `apx.css` siguen **congelados**: si necesitas
tocarlos, me lo pides. **`src/styles/index.css` es zona compartida**: la Faena 2 le está añadiendo la
base de `.btn-secondary`. Si te falta una clase base, **pídemela**, no la escribas por tu cuenta.

## Reglas de sólo lectura (§3.2 del spec)

Sonda pasiva primero (`getComputedStyle` para localizar el elemento antes de pulsar). Permitido: navegar,
pulsar pestañas, chips y conmutadores de vista, abrir un modal de un registro existente. **Nunca:**
teclear en un campo, guardar, `+ Nuevo…`, botones de cambio de estado ni aspas de borrado. Listener de
red antes de cualquier clic que no sea un enlace. **Si dudas, preguntas ANTES.**

## Reglas de la ronda

1. **Acuse primero:** `RECIBIDO` + reformulación con tus palabras.
2. **Reporta** con `herdr agent prompt wA:p5 "FAENA <n> <HITO|PREGUNTA|BLOQUEO>: <mensaje>"` al cerrar
   cada bloque, y deja el pane `idle`.
3. **Nada se fusiona a `main` sin OK explícito** de Arian a través del coordinador. Pushea y avísame: la
   PR la abro yo.
4. **Verificación:** `npm test`, `npx tsc --noEmit`, `npm run lint` pegados en el reporte.
